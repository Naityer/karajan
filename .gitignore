# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.env

# SQLite WAL sidecar files (transient; the .db itself is intentionally tracked)
*.db-wal
*.db-shm

# Runtime config override written by PUT /config
data/active_config.json

# First-run onboarding marker (POST /setup/complete)
data/.setup_complete

# Trial reports
data/trial_reports/
data/audit_reports/
